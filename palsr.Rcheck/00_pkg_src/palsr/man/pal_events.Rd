% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{pal_events}
\alias{pal_events}
\title{Construct a validated dyadic-event table}
\usage{
pal_events(
  data,
  actor1 = "actor1",
  actor2 = "actor2",
  time = "time",
  lon = "lon",
  lat = "lat",
  drop_self = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} with one row per dyadic event.}

\item{actor1, actor2}{Column names (length-1 character) identifying the two actors
involved in each event. The pair is treated as unordered.}

\item{time}{Column name of the event time. Must be a \code{Date}, or coercible to one via
\code{\link[=as.Date]{as.Date()}}.}

\item{lon, lat}{Column names of the event longitude and latitude, in decimal degrees.}

\item{drop_self}{Logical; drop events whose two actors are identical (default \code{TRUE}).}
}
\value{
An object of class \code{pal_events} (a \code{data.frame} subclass) with canonical
columns \code{actor1}, \code{actor2}, \code{time}, \code{lon}, \code{lat}, sorted by \code{time}.
}
\description{
\code{pal_events()} builds the core data object used throughout \pkg{palsr}: a table of
dyadic interaction events, each involving two actors at a known time and location.
Projected Actor Locations are computed from these histories.
}
\details{
Longitudes must lie in \verb{[-180, 180]} and latitudes in \verb{[-90, 90]}. Rows with
missing actor, time, or coordinate values are dropped with a message.
}
\examples{
df <- data.frame(
  from = c("A", "A", "B"),
  to   = c("B", "C", "C"),
  when = as.Date(c("2001-01-01", "2001-06-01", "2002-01-01")),
  x    = c(7.1, 8.0, 7.5),
  y    = c(9.0, 9.4, 10.1)
)
ev <- pal_events(df, actor1 = "from", actor2 = "to",
                 time = "when", lon = "x", lat = "y")
ev

}
