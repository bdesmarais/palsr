% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance.R
\name{predict_event_locations}
\alias{predict_event_locations}
\title{Predict dyadic event locations}
\usage{
predict_event_locations(
  events,
  targets,
  params,
  alter_weight = c("normalized", "legacy"),
  eps = 0.01
)
}
\arguments{
\item{events}{A \link{pal_events} object supplying the histories.}

\item{targets}{A \code{data.frame} with columns \code{actor1}, \code{actor2}, \code{time}, and optionally
\code{lon}/\code{lat} giving the observed event location (for error scoring).}

\item{params}{A \link{pals_params} or fitted \link{estimate_pals} object.}

\item{alter_weight, eps}{See \link{project_pal}.}
}
\value{
\code{targets} augmented with \code{pred_lon}, \code{pred_lat}, and (if observed \code{lon}/\code{lat}
were supplied) \code{error_km}, the Haversine distance between predicted and observed
locations. Predictions are \code{NA} when both actors lack usable history.
}
\description{
For each dyadic target (a pair of actors at a time), predicts the interaction
location as the mean of the two actors' Projected Actor Locations. Optionally scores
the prediction against an observed location.
}
\examples{
ev  <- simulate_conflict_events(n_actors = 10, n_events = 300, seed = 1)
fit <- estimate_pals(ev, model = "one")
tg  <- ev[ev$time > as.Date("2012-01-01"), ]
head(predict_event_locations(ev, tg, fit))

}
