% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_conflict_events}
\alias{simulate_conflict_events}
\title{Simulate dyadic conflict events between moving actors}
\usage{
simulate_conflict_events(
  n_actors = 30,
  n_events = 2000,
  years = 2000:2016,
  drift = 0.18,
  jitter = 0.25,
  decay = 2,
  bbox = c(2.7, 14.7, 4, 13.9),
  seed = NULL
)
}
\arguments{
\item{n_actors}{Number of actors (default 30).}

\item{n_events}{Number of dyadic events (default 2000).}

\item{years}{Integer vector of years to span (default \code{2000:2016}).}

\item{drift}{Standard deviation (decimal degrees per year) of each actor's directional
drift; larger values make actors more mobile (default \code{0.18}).}

\item{jitter}{Standard deviation (decimal degrees) of event-location noise around the
midpoint of the two actors' current locations (default \code{0.25}).}

\item{decay}{Spatial interaction scale (degrees): partners are chosen with probability
proportional to \code{exp(-distance / decay)}, so nearby actors interact more (default \code{2}).}

\item{bbox}{Bounding box \code{c(lon_min, lon_max, lat_min, lat_max)} for actor home
locations (default approximately Nigeria).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A \link{pal_events} object with columns \code{actor1}, \code{actor2}, \code{time}, \code{lon}, \code{lat}.
}
\description{
Generates a synthetic dataset of dyadic interaction events with the qualitative
structure PALS is designed for: a set of armed-group-like actors, each following a
slowly drifting spatial trajectory, that interact preferentially with nearby actors.
Useful for examples, tests, and the package vignette. The geographic frame
approximates Nigeria, echoing the application in Kim, Liu and Desmarais (2023).
}
\examples{
ev <- simulate_conflict_events(n_actors = 12, n_events = 400, seed = 42)
ev
summary(ev)

}
