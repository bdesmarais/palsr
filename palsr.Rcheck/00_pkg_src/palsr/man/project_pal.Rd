% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{project_pal}
\alias{project_pal}
\title{Project the location of a single actor}
\usage{
project_pal(
  events,
  actor,
  predict_time,
  params,
  alter_weight = c("normalized", "legacy"),
  eps = 0.01
)
}
\arguments{
\item{events}{A \link{pal_events} object.}

\item{actor}{The focal actor id (length-1 character).}

\item{predict_time}{A \code{Date} (or vector of \code{Date}s) at which to project. Only events
strictly earlier than each prediction time are used.}

\item{params}{A \link{pals_params} object or a fitted \link{estimate_pals} object.}

\item{alter_weight}{\code{"normalized"} (default) uses the paper's normalized alter
weights; \code{"legacy"} reproduces the original replication code (an un-normalized sum
of alter coordinates). See \code{DECISIONS.md}.}

\item{eps}{Numerical offset inside each age weight (default \code{0.01}).}
}
\value{
A \code{data.frame} with one row per prediction time and columns \code{actor}, \code{time},
\code{lon}, \code{lat}, \code{n_focal}, \code{n_alter}, \code{has_history}. \code{lon}/\code{lat} are \code{NA} when the
actor has no prior events.
}
\description{
Computes the Projected Actor Location (PAL) for one actor at one or more prediction
times, given a parameter set.
}
\examples{
ev <- simulate_conflict_events(n_actors = 8, n_events = 200, seed = 1)
p  <- pals_params(alpha = 0.9, model = "one")
project_pal(ev, actor = "G01", predict_time = as.Date("2010-12-01"), params = p)

}
