% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{pool_rubin}
\alias{pool_rubin}
\title{Pool estimates across imputations with Rubin's Rules}
\usage{
pool_rubin(estimates, variances, df = FALSE, dfcom = Inf)
}
\arguments{
\item{estimates}{Numeric vector of per-imputation point estimates \eqn{Q_j}.}

\item{variances}{Numeric vector of per-imputation variances \eqn{U_j}
(the squared standard errors), the same length as \code{estimates}.}

\item{df}{Logical; if \code{TRUE}, also return the Barnard-Rubin adjusted degrees of
freedom and a corresponding two-sided p-value for \code{H0: Q = 0}. Default \code{FALSE}
reproduces the source code's normal-based pooling.}

\item{dfcom}{Complete-data degrees of freedom, used only when \code{df = TRUE}
(default \code{Inf}, the large-sample limit).}
}
\value{
A one-row \code{data.frame} with the pooled estimate \code{qbar}, within-imputation
variance \code{ubar}, between-imputation variance \code{b}, total variance \code{t}, standard
error \code{se}, fraction of missing information \code{fmi}, and (if \code{df = TRUE}) \code{df} and
\code{p.value}.
}
\description{
Combines per-imputation point estimates and variances of a scalar quantity into a
single pooled estimate with a variance that accounts for both within- and
between-imputation uncertainty (Rubin, 1987). Use it to pool estimands computed on
each bootstrap/imputation replicate of \code{\link[=bootstrap_pals]{bootstrap_pals()}} — for example a
regression coefficient from a dyadic model fit on each replicate's PAL distances.
}
\details{
With \eqn{m} imputations,
\deqn{\bar Q = \tfrac1m \sum_j Q_j,\quad \bar U = \tfrac1m \sum_j U_j,\quad
        B = \tfrac{1}{m-1}\sum_j (Q_j-\bar Q)^2,}
and total variance \eqn{T = \bar U + (1 + 1/m) B}. The fraction of missing
information is \eqn{(1 + 1/m)B / T}. When \code{df = TRUE}, the Barnard-Rubin (1999)
small-sample degrees of freedom are used.
}
\examples{
# Five imputations of a coefficient and its variance.
q <- c(1.10, 0.95, 1.20, 1.05, 0.98)
u <- c(0.04, 0.05, 0.045, 0.038, 0.052)
pool_rubin(q, u)
pool_rubin(q, u, df = TRUE, dfcom = 100)

}
\references{
Rubin, D. B. (1987). \emph{Multiple Imputation for Nonresponse in Surveys}. Wiley.

Barnard, J. and Rubin, D. B. (1999). Small-sample degrees of freedom with multiple
imputation. \emph{Biometrika}, 86(4), 948-955.
}
\seealso{
\code{\link[=bootstrap_pals]{bootstrap_pals()}}.
}
