% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_pals}
\alias{bootstrap_pals}
\title{Nonparametric bootstrap for PALS estimates and projections}
\usage{
bootstrap_pals(
  events,
  R = 50,
  model = c("four", "one"),
  predict_time = NULL,
  actors = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{events}{A \link{pal_events} object.}

\item{R}{Number of bootstrap replicates (default \code{50}; the paper uses \code{10}).}

\item{model}{\code{"four"} or \code{"one"} (passed to \link{estimate_pals}).}

\item{predict_time}{Optional \code{Date} (or vector of \code{Date}s). When supplied, every
replicate also projects all actors at these times, so the spread of projected
coordinates across replicates is available for confidence regions and pooling.}

\item{actors}{For projection, which actors to project (default: all in \code{events}).}

\item{seed}{Optional integer seed; replicate \code{r} uses \code{seed + r} so the whole run
is reproducible.}

\item{...}{Further arguments passed to \link{estimate_pals} (e.g. \code{aggregate},
\code{alter_weight}, \code{method}, \code{control}).}
}
\value{
An object of class \code{pals_boot} with components:
\describe{
\item{\code{estimates}}{An \code{R}-row \code{data.frame} of replicate parameter estimates.}
\item{\code{estimate}}{The point estimate on the full sample (an \link{estimate_pals} fit).}
\item{\code{projections}}{If \code{predict_time} was given, a \code{data.frame} of projected
\code{lon}/\code{lat} for every actor-time-replicate combination; otherwise \code{NULL}.}
\item{\code{R}, \code{model}, \code{call}}{Bookkeeping.}
}
Methods: \code{\link[=print]{print()}}, \code{\link[=summary]{summary()}} (bootstrap SEs / percentile intervals), and
\code{\link[=coef]{coef()}} (the full-sample point estimate).
}
\description{
Quantifies uncertainty in PALS parameter estimates and in projected actor
locations by resampling events with replacement and re-estimating the model on
each bootstrap replicate, following Kim, Liu and Desmarais (2023). Each replicate
yields a parameter vector and (optionally) a set of Projected Actor Locations; the
collection of replicate PAL sets can be treated as multiple imputations and pooled
with Rubin's Rules (see \code{\link[=pool_rubin]{pool_rubin()}}).
}
\details{
Resampling is over rows of \code{events} (the nonparametric event bootstrap).
Duplicated events are kept as ordinary repeated events. Replicates whose
optimizer fails to converge are retained but flagged via the \code{convergence}
column of \code{estimates}.
}
\examples{
ev <- simulate_conflict_events(n_actors = 8, n_events = 200, seed = 1)
bt <- bootstrap_pals(ev, R = 10, model = "one", seed = 1)
summary(bt)

}
\seealso{
\code{\link[=estimate_pals]{estimate_pals()}}, \code{\link[=pool_rubin]{pool_rubin()}}.
}
