% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance.R
\name{pal_distance}
\alias{pal_distance}
\title{Dyadic distance between Projected Actor Locations}
\usage{
pal_distance(
  events,
  dyads,
  params,
  transform = c("none", "log"),
  offset = 0.01,
  alter_weight = c("normalized", "legacy"),
  eps = 0.01
)
}
\arguments{
\item{events}{A \link{pal_events} object.}

\item{dyads}{A \code{data.frame} with columns \code{actor1}, \code{actor2}, \code{time}.}

\item{params}{A \link{pals_params} or fitted \link{estimate_pals} object.}

\item{transform}{\code{"none"} (default) returns distance in km; \code{"log"} returns
\code{log(distance + offset)}, as used for interstate-conflict-style specifications.}

\item{offset}{Offset added before logging (default \code{0.01}).}

\item{alter_weight, eps}{See \link{project_pal}.}
}
\value{
\code{dyads} augmented with \code{pal_distance} (and, for \code{transform = "log"},
\code{pal_log_distance}).
}
\description{
Builds the dyadic distance covariate used to model interaction likelihood: the
Haversine distance between the two actors' Projected Actor Locations.
}
\examples{
ev  <- simulate_conflict_events(n_actors = 8, n_events = 200, seed = 1)
fit <- estimate_pals(ev, model = "one")
dy  <- data.frame(actor1 = "G01", actor2 = "G02",
                  time = as.Date("2012-12-01"))
pal_distance(ev, dy, fit)

}
