% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{estimate_pals}
\alias{estimate_pals}
\title{Estimate PALS parameters}
\usage{
estimate_pals(
  events,
  fit_events = NULL,
  model = c("four", "one"),
  start = NULL,
  method = NULL,
  aggregate = c("mean", "sum"),
  alter_weight = c("normalized", "legacy"),
  eps = 0.01,
  radius = 6371.0088,
  cutoff = c("day", "month", "year"),
  control = list()
)
}
\arguments{
\item{events}{A \link{pal_events} object providing the actor histories.}

\item{fit_events}{Optional \code{data.frame} of target events to fit against (needs
\code{actor1}, \code{actor2}, \code{time}, \code{lon}, \code{lat}). Defaults to all of \code{events}; events with
no usable history contribute nothing and are ignored.}

\item{model}{\code{"four"} (full: focal + alter histories, estimates \code{alpha}, \code{beta},
\code{gamma}, \code{eta}) or \code{"one"} (focal-only; estimates \code{alpha}, with \code{pi = 0}).}

\item{start}{Optional numeric starting vector on the optimizer's scale
(\code{c(gamma, eta, log_alpha, log_beta)} for the four-parameter model, \code{log_alpha} for
the one-parameter model). Sensible defaults are used if \code{NULL}.}

\item{method}{Optimizer method passed to \link[stats:optim]{stats::optim} (\code{"Nelder-Mead"} for the
four-parameter model, \code{"Brent"} for the one-parameter model by default).}

\item{aggregate}{\code{"mean"} (default) or \code{"sum"} of per-event distances.}

\item{alter_weight, eps, cutoff}{See \link{project_pal}.}

\item{radius}{Sphere radius for the Haversine objective (km).}

\item{control}{A list of control parameters for \link[stats:optim]{stats::optim}.}
}
\value{
An object of class \code{pals_fit} with components \code{params} (estimated
\link{pals_params}), \code{model}, \code{objective} (minimized mean/sum distance), \code{n_used}
(events contributing), \code{convergence}, \code{optim} (raw optimizer output), \code{events},
\code{settings}, and \code{call}. Methods: \code{\link[=print]{print()}}, \code{\link[=summary]{summary()}}, \code{\link[=coef]{coef()}}, \code{\link[=predict]{predict()}},
\code{\link[=plot]{plot()}}.
}
\description{
Estimates the PALS smoothing parameters by minimizing the mean great-circle
(Haversine) distance between observed event locations and the locations predicted
from each event's preceding history ("marching forward": every prediction uses only
events strictly earlier than the event being predicted).
}
\examples{
ev  <- simulate_conflict_events(n_actors = 10, n_events = 300, seed = 1)
fit <- estimate_pals(ev, model = "one")
fit
coef(fit)

}
\seealso{
\code{\link[=project_pals]{project_pals()}}, \code{\link[=predict_event_locations]{predict_event_locations()}}, \code{\link[=bootstrap_pals]{bootstrap_pals()}}.
}
