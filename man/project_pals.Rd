% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{project_pals}
\alias{project_pals}
\title{Project locations for multiple actors}
\usage{
project_pals(
  events,
  actors = NULL,
  predict_time,
  params,
  alter_weight = c("normalized", "legacy"),
  eps = 0.01,
  cutoff = c("day", "month", "year")
)
}
\arguments{
\item{events}{A \link{pal_events} object.}

\item{actors}{Character vector of actor ids. Defaults to all actors in \code{events}.}

\item{predict_time}{A \code{Date} or vector of \code{Date}s.}

\item{params}{A \link{pals_params} object or a fitted \link{estimate_pals} object.}

\item{alter_weight}{\code{"normalized"} (default) uses the paper's normalized alter
weights; \code{"legacy"} reproduces the original replication code (an un-normalized sum
of alter coordinates). See \code{DECISIONS.md}.}

\item{eps}{Numerical offset inside each age weight (default \code{0.01}).}

\item{cutoff}{History granularity: \code{"day"} (default) uses events strictly before
\code{predict_time}; \code{"month"} uses events before the 1st of that month; \code{"year"} uses
events before Jan 1 of that year (the source's Dec-1 yearly convention). Ages are
always measured from \code{predict_time}.}
}
\value{
A \code{data.frame} with columns \code{actor}, \code{time}, \code{lon}, \code{lat}, \code{n_focal},
\code{n_alter}, \code{has_history} (one row per actor-time combination).
}
\description{
Computes Projected Actor Locations for a set of actors at one or more prediction
times (an actor-by-time grid).
}
\examples{
ev <- simulate_conflict_events(n_actors = 10, n_events = 300, seed = 1)
p  <- pals_params(alpha = 0.9, beta = 0.2, gamma = -10, eta = -10)
pal <- project_pals(ev, predict_time = as.Date("2010-12-01"), params = p)
head(pal)

}
